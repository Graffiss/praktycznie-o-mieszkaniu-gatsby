import styled from "styled-components"

const Button = styled.button`
  background-color: #fbd35d;
  border: none;
  padding: 20px 50px;
  color: black;
  font-size: 18px;
  font-weight: 600;
  flex-shrink: 0;
  border-radius: 10px;
  outline: none;

  :hover {
    box-shadow: 0px 20px 25px -10px rgba(0, 0, 0, 1);
  }
`

export default Button
